'use babel';

import TryMakePluginsWordCountView from '../lib/try-make-plugins-word-count-view';

describe('TryMakePluginsWordCountView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
